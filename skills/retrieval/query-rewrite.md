# Query Rewrite

## 职责

负责在不改变业务含义的前提下改写用户问题，提高向量召回和关键词召回的稳定性。

## 当前实现位置

- `backend/app/rag/pipeline.py`：当前仅做基础空白字符规整。
- `backend/app/rag/retriever.py`：改写后的查询同时进入向量检索与 BM25 检索。

## 扩展方向

- 为常见政务词添加轻量规则改写。
- 可接入小模型生成检索查询，但需要保留原始问题和改写问题到 RAG Debug。
- 改写结果应进入检索日志，便于排查召回失败。
